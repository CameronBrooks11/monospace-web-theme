name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "demo/demo.html" # trigger when demo changes
      - ".github/workflows/deploy.yml" # trigger when workflow changes

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the repo
      - name: Checkout source
        uses: actions/checkout@v4

      # 2. Configure GitHub Pages
      - name: Set up Pages
        uses: actions/configure-pages@v4

      # 3. Copy demo/demo.html to root _site/index.html
      - name: Prepare deployment content
        run: |
          mkdir -p _site
          cp demo/demo.html _site/index.html

      # 4. Upload everything under _site/ as the Pages artifact
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      # 5. Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
